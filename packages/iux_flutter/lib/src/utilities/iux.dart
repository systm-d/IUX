/// Package-level metadata intended for diagnostics and the development catalog.
abstract final class Iux {
  /// The stable package identifier used by IUX consumers.
  static const String packageName = 'iux_flutter';

  /// The development version represented by this source tree.
  ///
  /// Keep this value aligned with the version in `pubspec.yaml`.
  static const String version = '0.1.0-dev.4';
}
