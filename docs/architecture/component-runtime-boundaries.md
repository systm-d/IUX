# Component runtime boundaries

Foundations are pure values. Semantic colors resolve through the theme. The
accessibility runtime reads platform preferences; motion and feedback consume
those preferences. Components compose these layers and never own application
operation state.
